import serial
import time
rcv=serial.Serial("/dev/ttyAMA0",9600)
rcv=serial.Serial("/dev/ttyAMA0",9600)
while True:
#	if (rcv.inWaiting()>0) :
	raw_data=rcv.readline()
	rcv.close()
	if '$GPGGA' in raw_data:
		print(raw_data)
		datas=raw_data.split(',')
		for i in range(0,len(datas)-1):
				print(datas[i])
				print('\r\n')
	time.sleep(1)

